before_install:
  - sdk install java 11.0.10-open
  - sdk use java 11.0.10-open
  -
install:
  - echo "Running a custom install command"
  - ./gradlew clean -Pgroup=com.github.api-generator-playground -Pversion=main-a6d1b9f45a-1 jar publishToMavenLocal